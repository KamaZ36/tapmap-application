# 🚕 TapMap Application

Современная система управления заказами такси с поддержкой REST API и Telegram ботов. Проект демонстрирует применение Clean Architecture, CQRS паттерна и Event-Driven Architecture.

## 📋 Описание

TapMap - это полнофункциональная платформа для управления заказами такси, которая включает:

- **REST API** для управления заказами, пользователями и водителями
- **Telegram боты** для пользователей и водителей
- **Систему геолокации** с поддержкой PostGIS
- **Автоматический подбор водителей** на основе ближайшего расположения
- **Event-driven архитектуру** для асинхронной обработки событий
- **Систему управления сменами** для водителей

## 🏗️ Архитектура

Проект построен на принципах **Чистой архитектуры** с четким разделением на слои:

```
app/
├── domain/              # Доменный слой (бизнес-логика)
│   ├── entities/       # Доменные сущности
│   ├── value_objects/  # Объекты-значения
│   ├── events/         # Доменные события
│   └── exceptions/     # Доменные исключения
├── application/        # Слой приложения
│   ├── commands/       # Команды (CQRS)
│   ├── queries/        # Запросы (CQRS)
│   ├── dtos/           # Data Transfer Objects
│   └── services/       # Сервисы приложения
├── infrastructure/     # Инфраструктурный слой
│   ├── database/       # Работа с БД (SQLAlchemy)
│   ├── repositories/  # Реализация репозиториев
│   ├── services/       # Внешние сервисы (геокодер, роутинг)
│   └── redis/          # Redis клиент
├── api/                # API слой (FastAPI)
│   └── v1/
│       ├── entrypoints/# REST API endpoints
│       └── schemas/    # Pydantic схемы
└── bots/               # Telegram боты
    ├── user_tg_bot/    # Бот для пользователей
    └── driver_tg_bot/  # Бот для водителей
```

### Используемые паттерны

- **CQRS** - разделение команд и запросов через Mediator
- **Repository Pattern** - абстракция доступа к данным
- **Domain-Driven Design** - богатая доменная модель
- **Event-Driven Architecture** - асинхронная обработка событий через message broker
- **Dependency Injection** - через Dishka контейнер

## 🛠️ Технологический стек

### Backend
- **Python 3.12** - основной язык
- **FastAPI** - веб-фреймворк для REST API
- **SQLAlchemy 2.0** - ORM с async поддержкой
- **PostgreSQL + PostGIS** - база данных с поддержкой геолокации
- **Redis** - кеширование и message broker
- **Alembic** - миграции БД

### Telegram боты
- **aiogram 3.x** - фреймворк для Telegram ботов
- **FastStream** - обработка событий из message broker

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **Poetry** - управление зависимостями
- **Pytest** - тестирование

### Дополнительные библиотеки
- **Dishka** - Dependency Injection контейнер
- **Pydantic** - валидация данных
- **Shapely** - работа с геометрией
- **GeoAlchemy2** - поддержка PostGIS в SQLAlchemy
- **JWT** - аутентификация

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Poetry (опционально, для локальной разработки)

### Установка и запуск

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd tapmap-application
```

2. **Создайте файл `.env`** (пример конфигурации .env.example)

3. **Запустите через Docker Compose**
```bash
docker-compose up -d
```

4. **Примените миграции**
```bash
docker exec -it api poetry run alembic upgrade head
```

5. **API будет доступен по адресу**
```
http://localhost:8000
```

6. **Swagger документация**
```
http://localhost:8000/docs
```

### Локальная разработка

```bash
# Установка зависимостей
poetry install

# Запуск API
poetry run uvicorn app.api.main:create_app --reload

# Запуск тестов
poetry run pytest

# Запуск с покрытием
poetry run pytest --cov=app --cov-report=html
```

## 📚 Основные возможности

### Для пользователей
- ✅ Регистрация и аутентификация
- ✅ Создание заказов с указанием адреса или координат
- ✅ Добавление точек маршрута
- ✅ Отслеживание статуса заказа
- ✅ История заказов
- ✅ Telegram бот для удобного управления

### Для водителей
- ✅ Регистрация водителя
- ✅ Управление сменами (выход/уход со смены)
- ✅ Обновление геолокации
- ✅ Получение заказов (автоматический подбор)
- ✅ Управление статусами заказов
- ✅ Telegram бот для работы с заказами

### Для администраторов
- ✅ Управление пользователями (блокировка/разблокировка)
- ✅ Управление городами
- ✅ Просмотр всех заказов

## 🧪 Тестирование

Проект включает unit и integration тесты:

```bash
# Все тесты
poetry run pytest

# Только unit тесты
poetry run pytest tests/unit/

# Только integration тесты
poetry run pytest tests/integration/

# С покрытием кода
poetry run pytest --cov=app --cov-report=html
```

Подробнее о тестировании: [tests/README.md](tests/README.md)

## 📁 Структура проекта

```
tapmap-application/
├── alembic/              # Миграции БД
├── app/
│   ├── api/              # REST API
│   ├── application/      # Слой приложения (команды, запросы)
│   ├── bots/             # Telegram боты
│   ├── core/             # Ядро приложения (конфигурация, DI)
│   ├── dispatcher/       # Обработка событий
│   ├── domain/           # Доменный слой
│   ├── infrastructure/   # Инфраструктура (БД, внешние сервисы)
│   └── utils/            # Утилиты
├── docker/               # Docker конфигурации
├── tests/                # Тесты
├── docker-compose.yml    # Docker Compose конфигурация
├── Dockerfile            # Docker образ
├── pyproject.toml        # Зависимости Poetry
└── README.md            # Этот файл
```

## 🔄 Поток работы заказа

1. **Создание заказа** - пользователь создает черновик заказа
2. **Добавление точек** - пользователь добавляет точки маршрута
3. **Подтверждение** - заказ подтверждается и переходит в статус поиска водителя
4. **Подбор водителя** - система автоматически находит ближайшего свободного водителя
5. **Назначение** - водитель назначается на заказ
6. **Выполнение** - отслеживание статусов: ожидание водителя → водитель прибыл → поездка → завершено

## 🎯 Особенности реализации

### CQRS через Mediator
Все команды и запросы обрабатываются через централизованный Mediator, что обеспечивает:
- Единую точку входа для бизнес-логики
- Легкое добавление middleware (логирование, валидация)
- Тестируемость и изоляцию

### Доменные события
Сущности могут генерировать доменные события, которые обрабатываются асинхронно через message broker.

### Геолокация
- Поддержка PostGIS для работы с геоданными
- Автоматическое определение города по координатам
- Расчет расстояний и времени маршрута

### Асинхронность
Весь код написан с использованием async/await для максимальной производительности.

## 📝 API Endpoints

Основные endpoints:

- `POST /auth/login` - Аутентификация
- `POST /auth/refresh` - Обновление токена
- `GET /users/{id}` - Получить пользователя
- `POST /orders/draft` - Создать черновик заказа
- `POST /orders/draft/{id}/point` - Добавить точку маршрута
- `POST /orders/draft/{id}/confirm` - Подтвердить заказ
- `GET /orders/{id}` - Получить заказ
- `POST /drivers/shift/start` - Выйти на смену
- `POST /drivers/location` - Обновить геолокацию

Полная документация доступна в Swagger UI: `http://localhost:8000/docs`

## 🔐 Безопасность

- JWT токены для аутентификации
- Валидация входных данных через Pydantic
- Доменные исключения для обработки ошибок
- Разделение прав доступа через роли

## 🚧 Что можно улучшить

- [ ] Автоматическая публикация доменных событий после commit транзакции
- [ ] CI/CD pipeline
- [ ] Увеличение покрытия тестами
- [ ] Мониторинг и логирование (ELK, Prometheus)
- [ ] Rate limiting для API

---

⭐ Если проект был полезен, поставьте звезду!

